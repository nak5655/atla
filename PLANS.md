# Plan

- [x] 現状の `Typing` 依存箇所と `Analyze` の型推論不足箇所を特定する。
- [x] `Typing` モジュールを削除し、プロジェクト参照とコンパイルパイプラインから取り除く。
- [x] `Analyze` に関数引数宣言・戻り値型統一など不足している型推論を実装する。
- [x] 変更を検証するテストを追加・更新する。
- [x] 利用可能な検証コマンドを実行する（`dotnet` 未導入のためテスト実行不可）。
