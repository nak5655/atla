# Plan

- [x] layoutモジュールのコンパイルエラー原因を特定する。
- [x] `Lowering/Layout.fs` を現行の HIR/MIR 定義に合わせて書き直し、型不整合を解消する。
- [x] レイアウト結果（命令列と戻り値命令）の回帰テストを追加する。
- [x] 利用可能な範囲でテスト／検証コマンドを実行する（`dotnet` が未インストールのため実行不可）。
