# Build Resolver

## Source paths

- `src/Atla.Build/Resolver.fs`
- `src/Atla.Core/DependencyLoader.fs`

## Current policy

The build resolver owns project/dependency discovery at build-tool level. Compiler-side dependency metadata loading is documented in `notes/compiler/dependencies/`.
