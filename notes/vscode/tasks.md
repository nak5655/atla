# VSCode Tasks

## Source paths

- `vscode/package.json`
- `vscode/src/tasks.ts`

## Current policy

VSCode task integration delegates Atla build operations to the configured `atla` executable rather than duplicating build logic in TypeScript.
