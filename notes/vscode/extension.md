# VSCode Extension

## Source paths

- `vscode/package.json`
- `vscode/src/extension.ts`
- `vscode/language-configuration.json`

## Current policy

The extension contributes the Atla language, activates for `.atla` files, starts the configured language server, and registers Atla editor/build integrations.
