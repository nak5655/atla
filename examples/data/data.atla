import System'Console

data Line =
    { slope: Float
    , intercept: Float
    }

impl Line
    fn evaluate (x: Float): Float =
        slope * x + intercept

fn main (): () =
    let line = Line { slope = 2.0, intercept = -1.0 }
    5.0 line'evaluate. Console'WriteLine.
