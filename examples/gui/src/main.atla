import Avalonia.Application
import Avalonia.Controls.AppBuilder
import Avalonia.Themes.Fluent.FluentTheme

fn appMain (app: Application) (args: Array String): () = do
    app.Styles.Add (FluentTheme ())
    let window = Window ()
    window.Show ()
    app.Run window

fn main (args: Array String): () = do
    let config = AppBuilder.Configure<Application> ()
    config.UsePlatformDetect ()
    config.Start appMain args