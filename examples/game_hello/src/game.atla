import Stride'Engine'Game
import Stride'Graphics'SpriteBatch
import Stride'Games'GameTime

data FlappyGame =
    { _spriteBatch: Opt SpriteBatch }

impl FlappyGame as Game
    fn new: FlappyGame =
        FlappyGame { _spriteBatch = Opt'None }

    fn Initialize self: () =
        base'Initialize.

        self'_spriteBatch = base'GraphicsDevice SpriteBatch. Opt'Some.

    fn Update self (gameTime: GameTime): () =
        gameTime base'Update.