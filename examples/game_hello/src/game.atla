import Stride'Engine'Game

data FlappyGame =
    { _: Int }

impl FlappyGame as Game
    fn new: FlappyGame =
        FlappyGame { _ = 0 }

    fn Initialize (this: FlappyGame): () =
        base'Initialize.